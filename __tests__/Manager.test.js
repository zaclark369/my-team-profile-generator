const Manager = require('../lib/Manager.js');

test('this will test for Officer Number', () => {
    const employee = new Employee({name: 'John', id: "240", email: 'John@example.com', role: 'manager', office: '3'});
    
});